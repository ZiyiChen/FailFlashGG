<h1>Rekt#index</h1>
<%= form_tag(:action => 'show') do %>
<table>
    <tr>
      <td><%= label_tag(:name) %></td>
      <td><%= text_field_tag(:name) %></td>
    </tr>
   
    <tr>
      <td> </td>
      <td><%= submit_tag("Search") %></td>
    </tr>
  </table>
  <% end %>
